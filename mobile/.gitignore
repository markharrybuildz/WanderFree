# Expo / RN
node_modules/
.expo/
.expo-shared/
dist/
web-build/
ios/
android/
*.log

# Metro / TS
.metro-health-check*
expo-env.d.ts

# Env
.env
.env.local
.env.*.local

# Google Play service account key (Play Console upload creds) — never commit
google-service-account.json

# App Store Connect API key (.p8 upload creds) — never commit
asc-api-key.p8
*.p8
